// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xencode.h"

extern XEncode_Config XEncode_ConfigTable[];

XEncode_Config *XEncode_LookupConfig(u16 DeviceId) {
	XEncode_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XENCODE_NUM_INSTANCES; Index++) {
		if (XEncode_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XEncode_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XEncode_Initialize(XEncode *InstancePtr, u16 DeviceId) {
	XEncode_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XEncode_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XEncode_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

