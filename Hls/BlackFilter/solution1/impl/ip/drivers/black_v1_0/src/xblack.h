// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef XBLACK_H
#define XBLACK_H

#ifdef __cplusplus
extern "C" {
#endif

/***************************** Include Files *********************************/
#ifndef __linux__
#include "xil_types.h"
#include "xil_assert.h"
#include "xstatus.h"
#include "xil_io.h"
#else
#include <stdint.h>
#include <assert.h>
#include <dirent.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>
#include <unistd.h>
#include <stddef.h>
#endif
#include "xblack_hw.h"

/**************************** Type Definitions ******************************/
#ifdef __linux__
typedef uint8_t u8;
typedef uint16_t u16;
typedef uint32_t u32;
#else
typedef struct {
    u16 DeviceId;
    u32 Control_bus_BaseAddress;
} XBlack_Config;
#endif

typedef struct {
    u32 Control_bus_BaseAddress;
    u32 IsReady;
} XBlack;

/***************** Macros (Inline Functions) Definitions *********************/
#ifndef __linux__
#define XBlack_WriteReg(BaseAddress, RegOffset, Data) \
    Xil_Out32((BaseAddress) + (RegOffset), (u32)(Data))
#define XBlack_ReadReg(BaseAddress, RegOffset) \
    Xil_In32((BaseAddress) + (RegOffset))
#else
#define XBlack_WriteReg(BaseAddress, RegOffset, Data) \
    *(volatile u32*)((BaseAddress) + (RegOffset)) = (u32)(Data)
#define XBlack_ReadReg(BaseAddress, RegOffset) \
    *(volatile u32*)((BaseAddress) + (RegOffset))

#define Xil_AssertVoid(expr)    assert(expr)
#define Xil_AssertNonvoid(expr) assert(expr)

#define XST_SUCCESS             0
#define XST_DEVICE_NOT_FOUND    2
#define XST_OPEN_DEVICE_FAILED  3
#define XIL_COMPONENT_IS_READY  1
#endif

/************************** Function Prototypes *****************************/
#ifndef __linux__
int XBlack_Initialize(XBlack *InstancePtr, u16 DeviceId);
XBlack_Config* XBlack_LookupConfig(u16 DeviceId);
int XBlack_CfgInitialize(XBlack *InstancePtr, XBlack_Config *ConfigPtr);
#else
int XBlack_Initialize(XBlack *InstancePtr, const char* InstanceName);
int XBlack_Release(XBlack *InstancePtr);
#endif

void XBlack_Start(XBlack *InstancePtr);
u32 XBlack_IsDone(XBlack *InstancePtr);
u32 XBlack_IsIdle(XBlack *InstancePtr);
u32 XBlack_IsReady(XBlack *InstancePtr);
void XBlack_EnableAutoRestart(XBlack *InstancePtr);
void XBlack_DisableAutoRestart(XBlack *InstancePtr);


void XBlack_InterruptGlobalEnable(XBlack *InstancePtr);
void XBlack_InterruptGlobalDisable(XBlack *InstancePtr);
void XBlack_InterruptEnable(XBlack *InstancePtr, u32 Mask);
void XBlack_InterruptDisable(XBlack *InstancePtr, u32 Mask);
void XBlack_InterruptClear(XBlack *InstancePtr, u32 Mask);
u32 XBlack_InterruptGetEnabled(XBlack *InstancePtr);
u32 XBlack_InterruptGetStatus(XBlack *InstancePtr);

#ifdef __cplusplus
}
#endif

#endif
